/**
 * Trendtech Settings Store
 * Re-export from modular store structure
 * 
 * @deprecated Use React Query hooks instead:
 * - `useTrendtechSettings()` for settings
 * - `useTrendtechCategories()` for categories
 * - `useTrendtechBrands()` for brands
 * 
 * Import from: `@/features/settings/trendtech/hooks/use-trendtech`
 * 
 * Note: Keep config in Zustand, move sync data to React Query.
 * 
 * @module features/settings/trendtech/store
 */

export * from './store/index';
