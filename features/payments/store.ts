/**
 * Payments Store
 * Re-export from modular store structure
 * 
 * @deprecated Use React Query hooks instead:
 * - `usePayments()` for list
 * - `usePayment(id)` for single
 * - `usePaymentMutations()` for create/update/delete
 * 
 * Import from: `@/features/payments/hooks/use-payments`
 * 
 * This store will be removed in a future version.
 */

export * from './store/index';
