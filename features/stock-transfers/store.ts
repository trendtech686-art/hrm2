/**
 * Stock Transfers Store
 * Re-export from modular store structure
 * 
 * @deprecated Use React Query hooks instead:
 * - `useStockTransfers()` for list
 * - `useStockTransfer(id)` for single
 * - `useStockTransferMutations()` for create/update/delete
 * 
 * Import from: `@/features/stock-transfers/hooks/use-stock-transfers`
 * 
 * This store will be removed in a future version.
 */

export * from './store/index';
