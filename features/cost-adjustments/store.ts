/**
 * Cost Adjustments Store
 * Re-export from modular store structure
 * 
 * @deprecated Use React Query hooks instead:
 * - `useCostAdjustments()` for list
 * - `useCostAdjustment(id)` for single
 * - `useCostAdjustmentMutations()` for create/update/delete
 * 
 * Import from: `@/features/cost-adjustments/hooks/use-cost-adjustments`
 * 
 * This store will be removed in a future version.
 * 
 * @module features/cost-adjustments/store
 */

export * from './store/index';
