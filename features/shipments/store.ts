/**
 * Shipment Store
 * Re-export from modular store structure
 * 
 * @deprecated Use React Query hooks instead:
 * - `useShipments()` for list
 * - `useShipment(id)` for single
 * - `useShipmentMutations()` for create/update/delete
 * 
 * Import from: `@/features/shipments/hooks/use-shipments`
 * 
 * This store will be removed in a future version.
 */

export * from './store/index';