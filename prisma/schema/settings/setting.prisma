model Setting {
  systemId    String   @id @default(uuid())
  key         String
  value       Json
  type        String
  category    String
  description String?
  updatedAt   DateTime @updatedAt
  updatedBy   String?
  group       String

  @@unique([key, group])
  @@map("settings")
}
