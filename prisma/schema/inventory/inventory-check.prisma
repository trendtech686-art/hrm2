model InventoryCheck {
  systemId   String               @id
  id         String               @unique
  branchId   String
  employeeId String?
  checkDate  DateTime             @default(now())
  status     InventoryCheckStatus @default(DRAFT)
  notes      String?
  createdAt  DateTime             @default(now())
  updatedAt  DateTime             @updatedAt
  createdBy  String?
  updatedBy  String?
  
  // === MISSING FIELDS FROM types.ts (Phase 7) ===
  
  // Branch info (denormalized)
  branchSystemId       String?
  branchName           String?
  
  // Balance tracking
  balancedAt           DateTime?
  balancedBy           String?
  
  // Cancellation tracking
  cancelledAt          DateTime?
  cancelledBy          String?
  cancelledReason      String?
  
  // Items (stored as JSON for flexibility)
  checkItems           Json?          // Array of InventoryCheckItem with reason
  
  // Activity History
  activityHistory      Json?
  
  // === RELATIONS ===
  items      InventoryCheckItem[]

  @@index([branchId])
  @@index([status])
  @@map("inventory_checks")
}
